import React from 'react';

interface MovieFieldProps {
    children: React.ReactNode
}

const MovieField = ({children}: MovieFieldProps) => {
    return (
        <div>

        </div>
    );
};

export default MovieField;